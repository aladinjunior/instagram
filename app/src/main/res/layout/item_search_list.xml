<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginTop="16dp"
    android:layout_marginStart="16dp"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <de.hdodenhof.circleimageview.CircleImageView
        android:id="@+id/search_img_user"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        android:src="@drawable/ic_insta_profile_thin"
        app:civ_circle_background_color="@color/white"
        app:civ_border_width="1dp"
        app:civ_border_color="@color/white"
        android:layout_width="68dp"
        android:layout_height="68dp"/>

    <TextView
        android:textStyle="bold"
        android:id="@+id/search_text_user"
        style="@style/Theme.Instagram.TextViewBase"
        android:layout_width="0dp"
        android:layout_marginStart="16dp"
        android:text="@string/username"
        app:layout_constraintBottom_toTopOf="@id/search_text_name"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toEndOf="@id/search_img_user"
        />

    <TextView
        android:id="@+id/search_text_name"
        style="@style/Theme.Instagram.TextViewBase"
        android:layout_width="0dp"
        android:layout_marginStart="16dp"
        android:text="@string/username"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toEndOf="@id/search_img_user"
        app:layout_constraintTop_toBottomOf="@id/search_text_user"
        />

</androidx.constraintlayout.widget.ConstraintLayout>